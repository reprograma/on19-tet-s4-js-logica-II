console.clear ();

const frase = "  algo com espaços no começo e fim    ";

console.log("com espaços = " + frase);

// remova os espaços do início e do final usando trim

const resultado = frase.trim();
console.log("sem espaços = " + resultado);
