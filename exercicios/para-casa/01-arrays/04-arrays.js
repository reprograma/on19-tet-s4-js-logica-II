// Imprima o que for pedido nos comentários

const frase = "Eu sou uma frase com algumas palavras";

// usando indexOf, verifique o index de inicio da palavra 'sou'
{
  const answer = frase.indexOf('sou')
  console.log (answer)
}
// usando indexOf, verifique o index de inicio da palavra 'algumas'
{
  const answer = frase.indexOf('algumas')
  console.log (answer)
}
// usando indexOf, verifique o index de inicio da palavra 'cachorro'
{
  const answer = frase.indexOf('cachorro')
  console.log (answer)
}

const arrayExercicio = [
  "banana",
  "pera",
  "uva",
  "maça",
  "figo",
  "abacaxi",
  "caviar"
];


// usando indexOf, verifique a posição da palavra 'banana'
{
  const answer = arrayExercicio.indexOf('banana')
  console.log (answer)
}
// usando indexOf, verifique a posição da palavra 'figo'
{
  const answer = arrayExercicio.indexOf('figo')
  console.log (answer)
}
// usando indexOf, verifique a posição da palavra 'macarrão'
{
  const answer = arrayExercicio.indexOf('macarrão')
  console.log (answer)
}